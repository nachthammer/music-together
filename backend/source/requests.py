# The idea is to give requests a template so we dont have to look for the keys in the routes.py
# But I don't know yet how to do it the smart way

# Something like request = (username(min_length=4), password(min_length=8), some_optional_flag(optional=true))
